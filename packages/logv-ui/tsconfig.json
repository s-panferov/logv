{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"outDir": "lib" /* Redirect output structure to the directory. */,
		"rootDir": "src"
	},
	"references": [{ "path": "../logv-lib" }],
	"include": ["src/**/*"],
	"exclude": ["node_modules"]
}
